from datetime import datetime


def search_new_products(zone_id: str, bbox: list[float], since: datetime | None = None) -> list[dict]:
    """Placeholder STAC query function.

    Replace with a real CDSE STAC search call in v1 implementation.
    """
    _ = (zone_id, bbox, since)
    return []
